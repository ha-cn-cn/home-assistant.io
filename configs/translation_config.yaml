# 翻译配置
source_dir: "source"
output_dir: "source/zh"
translation_engine: "libre"  # libre/microsoft/google

# 术语管理
term_blacklist: "configs/term_blacklist.yaml"

# 性能配置
batch_size: 10
rate_limit: 1.0  # 请求间隔(秒)

# 质量阈值
length_ratio_threshold: 0.6
code_preserve_threshold: 0.95